export enum HttpMethods {
  DELETE = 'DELETE',
  GET = 'GET',
  OPTIONS = 'OPTIONS',
  POST = 'POST',
  PUT = 'PUT',
}

export enum QueryKey {
  COMMENTS = 'COMMENTS',
  POSTS = 'POSTS',
  REPLIES = 'REPLIES',
}

export enum Mode {
  EDIT,
  VIEW,
}
